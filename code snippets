# include <stdio.h>
void fun(int x)
{
    x = 30;
}
 
int main()   // hence it is call by value we can change the value in the function .
{
  int y = 20;
  fun(y);
  printf("%d", y);
  return 0;
}
output:
y=20;
